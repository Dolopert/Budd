# FinData Toolkit — Thai (SET) Market Dependencies
# Install: pip install -r requirements.txt

# === Data Fetching ===
requests>=2.31.0          # SET official public API (no key)
yfinance>=0.2.31          # Fallback + financial statements / history via {SYMBOL}.BK

# === Data Processing ===
pandas>=2.0.0
numpy>=1.24.0

# === Output ===
tabulate>=0.9.0           # Pretty table output
