Place the two attached sprite sheets in THIS folder:

    walk_sprite.png   (4-frame walking cat, horizontal strip)
    sit_sprite.png    (4-frame sitting cat, horizontal strip)

pet.py slices each sheet into frames automatically (frame count = width / height)
and removes the white background, so any solid-white-background strip works.
