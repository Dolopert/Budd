# Finance Slip — Claude Instructions

After every code edit to any file in this project, automatically run:
```
git add -A
git commit -m "Auto-update: <brief description of change>"
git push
```

Do this without asking for confirmation. The live app is hosted on GitHub Pages and updates automatically after each push.
